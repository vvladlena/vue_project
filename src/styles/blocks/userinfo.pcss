.userinfo{
    display: flex;
    /* flex-direction: column; */
    justify-content: center;
    align-items: flex-start;
}
.userinfo__name{
    font-weight: 800;
    font-size: 72px;
    line-height: 1;
    flex-basis: 400px;
}
.userinfo__title {
    text-align: right;
    font-size: 18px;
    /* color: rgba($text-color, .4); */
    font-weight: 600;
    margin-bottom: 20px;
    margin-right: 10px;
}